# Autenticação, Autorização e Matriz de Permissões — ECO CONTROL

Este documento estabelece o modelo de segurança, o ciclo de vida dos tokens de acesso e a estrutura de autorização baseada em papéis (RBAC) e por campo no sistema **ECO CONTROL**.

---

## 1. Fluxo de Autenticação 