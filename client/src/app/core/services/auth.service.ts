import {Injectable} from '@angular/core';import {BehaviorSubject,tap} from 'rxjs';import {ApiService} from './api.service';
export interface CurrentUser{id:string;email:string;full_name:string;role:string}
@Injectable({providedIn:'root'})export class AuthService{private userSubject=new BehaviorSubject<CurrentUser|null>(this.readUser());user$=this.userSubject.asObservable();constructor(private api:ApiService){}login(email:string,password:string){return this.api.post<any>('/auth/login',{email,password}).pipe(tap(r=>{localStorage.setItem('eco_token',r.access_token);localStorage.setItem('eco_user',JSON.stringify(r.user));this.userSubject.next(r.user)}))}logout(){localStorage.removeItem('eco_token');localStorage.removeItem('eco_user');this.userSubject.next(null)}get token(){return localStorage.getItem('eco_token')}get user(){return this.userSubject.value}get isAdmin(){return this.user?.role==='admin'}private readUser(){try{return JSON.parse(localStorage.getItem('eco_user')||'null')}catch{return null}}}
