import{Component,Input}from'@angular/core';import{ChartDatum}from'../../models/common';@Component({selector:'app-line-chart',templateUrl:'./line-chart.component.html',styleUrls:['./line-chart.component.css']})export class LineChartComponent{@Input()data:ChartDatum[]=[];get max(){return Math.max(1,...this.data.map(d=>d.value))}get points(){if(!this.data.length)return'';return this.data.map((d,i)=>`${30+(i*(520/Math.max(1,this.data.length-1)))},${190-(d.value/this.max*150)}`).join(' ')}}